import { sum } from '..';

describe('sum', () => {
    it('should sum', () => {
        expect(true).toBe(true);
        expect(sum(1, 2)).toBe(3);
        console.log('test');
        console.log('alpt');
    });
});
